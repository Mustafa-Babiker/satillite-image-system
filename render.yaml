services:
  - type: web
    name: satillite-image-system
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt -f https://download.pytorch.org/whl/cpu
    startCommand: gunicorn app:app --workers 1 --threads 2 --bind 0.0.0.0:$PORT
    autoDeploy: true
